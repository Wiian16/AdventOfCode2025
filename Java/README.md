# Advent of Code Java Submissions

This directory holds my submissions for each day of the Advent of Code challenge in Java. Each day will be a Gradle
submodule and will be runnable with `gradle :day##:run` from this directory or with `gradle run` from each 
sub-directory.
